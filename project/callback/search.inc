<?php
/**
 * @file
 * Callback for menu item: 'btr/project/%/%/%/search'
 */

/**
 * Menu callback for 'btr/project/%/%/%/search'
 */
function btrProject_search($origin, $project, $lng) {
  $output = "btrProject_search($origin, $project, $lng)";
  return $output;
}
