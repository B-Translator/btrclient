<?php
/**
 * @file
 * Callback for menu item: 'btr/project/%/%/%'
 */

/**
 * Menu callback for 'btr/project/%/%/%'
 */
function btrProject_random($origin, $project, $lng) {
  $output = "btrProject_random($origin, $project, $lng)";
  return $output;
}
