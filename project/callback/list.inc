<?php
/**
 * @file
 * Callback for menu item: 'btr/project/%/%/%/list'
 */

/**
 * Menu callback for 'btr/project/%/%/%/list'
 */
function btrProject_list($origin, $project, $lng) {
  $output = "btrProject_list($origin, $project, $lng)";
  return $output;
}
